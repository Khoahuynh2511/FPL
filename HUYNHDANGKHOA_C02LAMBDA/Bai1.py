# #Lambda bình phương của một số
# # squared_numbers = lambda x: x**2
# # print(squared_numbers(5))

# #Lọc số âm có trong mảng
# numbers = [1, -3, 4, -2, 7, -5, 9, -8, 10]
# negative_numbers = list(filter(lambda x: x < 0, numbers))
# print("Các số âm trong mảng là:", negative_numbers)

# #Sắp xếp độ dài tăng dần
# strings = ["apple", "banana", "cherry", "date", "elderberry", "fig", "grape"]
# sorted_strings = sorted(strings, key=lambda x: len(x))
# print("Danh sách sau khi sắp xếp tăng dần theo độ dài chuỗi:", sorted_strings)

